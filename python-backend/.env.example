# API 配置（支持 OpenAI / MiniMax / Mimo 或任何 OpenAI 协议兼容的 API）
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_NAME=gpt-4o

# Guardrail 使用独立模型（可选，不填则复用 MODEL_NAME）
# GUARDRAIL_MODEL_NAME=gpt-4o-mini

# 禁用追踪日志（可选，默认不启用）
OPENAI_TRACING_DISABLED=1
